-- ============================================================
-- ملف الترحيل — الأمان
-- منع تسجيل الدخول المتكرر لنفس الحساب من أكتر من جهاز في نفس الوقت
-- (باقي بنود الأمان — الخروج التلقائي وتغيير كلمة المرور — تُنفَّذ بالكامل من كود التطبيق
--  ومحتاجة السطر ده بس في القاعدة)
-- آمن للتشغيل على قاعدة بيانات موجودة بالفعل
-- ============================================================

alter table profiles add column if not exists current_session_token text;

-- المستخدم (أو المدير) يقدر يحدّث current_session_token — نفس سياسة profiles_update الموجودة أصلاً كافية،
-- والترغر protect_profile_privileges بيحمي role/is_active بس، فمش هيمنع تحديث العمود ده. مفيش داعي لأي تعديل إضافي.
